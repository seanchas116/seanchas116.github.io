---
title: Blog
---

h1 Blog

h2 Tags
ul.no-dots
  - blog.tags.each do |tag, articles|
    li= link_to "#{tag} (#{articles.size})", tag_path(tag)
h2 Articles
= partial :article_list, locals: {articles: page_articles}
